package org.openmrs.module.isanteplusreports.liquibase;

public class InitialiseReports {
	
	/*public void execute(Database database) throws CustomChangeException {
		JdbcConnection connection = (JdbcConnection) database.getConnection();
		
		// Hacking service, looks like at this stage module's services are not
		// yet ready
		try {
			new IsantePlusReportsServiceImpl().ReturnNumberPatients();
		} catch (DatabaseException e) {
			e.printStackTrace();
		} catch (SQLException e) {
			e.printStackTrace();
		}
	}*/
}
